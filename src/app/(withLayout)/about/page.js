export const metadata = {
    title: "About Page",
    description: "This is about page",
};
const AboutPage = () => {
    return (
        <div>
            <h2>about page with header footer</h2>
        </div>
    );
};

export default AboutPage;