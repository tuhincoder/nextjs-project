export const metadata = {
    title: 'Contact page',
    description: 'this is a contact page'
}

const ContactPage = () => {
    return (
        <div>
            <h2>contact page with header footer</h2>
        </div>
    );
};

export default ContactPage;